# Build EXE single package. 
# make sure debug is false, and browser only in false in app.py file first. 

python -m venv venv
venv\Scripts\Activate.ps1

pip install auto-py-to-exe
pip install .
python -m pip install pyinstaller --upgrade
python -m pip install auto-py-to-exe --upgrade

auto-py-to-exe

# import the json, or set the following settings.
# add the folder for templates, include, backup to be included in  one file, windows based.

exit